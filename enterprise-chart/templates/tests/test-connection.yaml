apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "enterprise-chart.fullname" . }}-test-connection"
  labels:
    {{- include "enterprise-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "enterprise-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
