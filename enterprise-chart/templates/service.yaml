apiVersion: v1
kind: Service
metadata:
  name: {{ include "enterprise-chart.fullname" . }}
  labels:
    {{- include "enterprise-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "enterprise-chart.selectorLabels" . | nindent 4 }}
